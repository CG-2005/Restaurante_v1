databases:
  - name: restaurante_v1db
    plan: free
    databaseName: restaurante
    user: mysite

services:
  - type: web
    plan: free
    name: restaurante_v1_web
    runtime: python
    buildCommand: './build.sh'
    startCommand: 'python -m gunicorn restaurante.asgi:application -k uvicorn.workers.UvicornWorker'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: restaurante_v1db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4